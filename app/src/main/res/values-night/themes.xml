<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Activemind" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>

    <style name="Theme.ActiveMind.Splash" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_screen</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/ic_launcher_adaptive_fore</item>
        <item name="android:windowSplashScreenBackground">@color/sky_blue_1</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.Activemind</item>
    </style>
</resources>